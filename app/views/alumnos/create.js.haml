-if @alumno.errors.any?
  :plain
    $('#resultado').html("#{escape_javascript( render 'new')}")
-else
  :plain
    $('#alumnos').html("#{escape_javascript( render 'maestras/lista_alumnos')}")
    $('#resultado').html("");
    $('#buscar').val("");